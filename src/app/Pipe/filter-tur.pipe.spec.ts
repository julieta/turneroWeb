import { FilterTurPipe } from './filter-tur.pipe';

describe('FilterTurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTurPipe();
    expect(pipe).toBeTruthy();
  });
});
